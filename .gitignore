# Terraform
**/.terraform/
*.tfstate.backup
*.tfplan
tfplan
.terraform.lock.hcl
terraform.tfvars
!terraform.tfvars.example

# Remote state files — ignored everywhere except bootstrap, which uses local state
*.tfstate
!aws/bootstrap/terraform.tfstate

# Packer
packer/output/
packer/packer_cache/

# Ansible
ansible/inventory/hosts
ansible/*.retry
*.log

# Secrets / local config
.env
*.pem
*.key
!*.key.example
wireguard/

# OS
.DS_Store
*.swp
