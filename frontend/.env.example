# Lamware React Frontend Configuration
# Copy to .env and fill in values (gitignored)

# FastAPI backend URL (Vite dev proxy handles this in dev mode)
VITE_API_BASE_URL=http://localhost:8001

# API key for authentication (empty = dev mode, no auth enforced)
VITE_API_KEY=
