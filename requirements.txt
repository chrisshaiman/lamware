# Runtime dependencies
# Install with: pip install -r requirements.txt

anthropic==0.102.0  # Claude API — vm_agent.py, vm_agent_cape.py
boto3==1.43.8       # AWS SDK — vm_agent_cape.py, sample_submitter.py, report_processor.py
Pillow==12.2.0      # PPM→PNG screenshot conversion — qemu_monitor.py
