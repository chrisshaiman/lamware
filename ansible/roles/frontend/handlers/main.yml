---
- name: Reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded

- name: Reload systemd and restart nginx
  ansible.builtin.systemd_service:
    name: nginx
    state: restarted
    daemon_reload: true
