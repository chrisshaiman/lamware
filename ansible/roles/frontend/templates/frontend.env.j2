# Frontend build-time environment variables
# These are baked into the Vite build at compile time (VITE_ prefix).
VITE_API_BASE_URL=
VITE_KEYCLOAK_URL=/auth
VITE_KEYCLOAK_REALM={{ keycloak_realm | default('lamware') }}
VITE_KEYCLOAK_CLIENT_ID={{ keycloak_client_id | default('lamware-web') }}
