---
# roles/cape/handlers/main.yml

- name: Restart Cape services
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
    daemon_reload: true
  loop:
    - cape
    - cape-web
    - cape-processor
  failed_when: false  # services may not be running yet on first provision
