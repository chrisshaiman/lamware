---
- name: Restart LiteLLM
  ansible.builtin.systemd:
    name: litellm
    state: restarted
    daemon_reload: true

- name: Restart LiteLLM socket bridge
  ansible.builtin.systemd:
    name: litellm-socket
    state: restarted
    daemon_reload: true
